# Protection CSRF basée sur la session (fonctionne sans JS, fiable derrière un reverse proxy).
# Auparavant en "stateless" : nécessitait un JS qui remplace le placeholder du token côté navigateur,
# ce qui ne fonctionnait pas en production derrière nginx.
framework:
    form:
        csrf_protection:
            token_id: submit
