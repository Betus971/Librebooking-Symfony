<form method="post" action="{{ path('app_blackout_series_delete', {'id': blackout_series.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blackout_series.id) }}">
    <button class="btn">Delete</button>
</form>
