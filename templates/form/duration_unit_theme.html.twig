{% block duration_unit_widget %}
    <div class="grid grid-cols-12 gap-2">
        <div class="col-span-7 md:col-span-8">
            {{ form_widget(form.children['value']) }}
        </div>
        <div class="col-span-5 md:col-span-4">
            {{ form_widget(form.children['unit']) }}
        </div>
    </div>
{% endblock %}
