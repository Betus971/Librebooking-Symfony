{% extends 'base.html.twig' %}

{% block title %}Layout index{% endblock %}

{% block body %}
    <h1>Layout index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Timezone</th>
                <th>LayoutType</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for layout in layouts %}
            <tr>
                <td>{{ layout.id }}</td>
                <td>{{ layout.name }}</td>
                <td>{{ layout.timezone }}</td>
                <td>{{ layout.layoutType }}</td>
                <td>
                    <a href="{{ path('app_layout_show', {'id': layout.id}) }}">show</a>
                    <a href="{{ path('app_layout_edit', {'id': layout.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_layout_new') }}">Create new</a>
{% endblock %}
