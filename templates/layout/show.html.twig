{% extends 'base.html.twig' %}

{% block title %}Layout{% endblock %}

{% block body %}
    <h1>Layout</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ layout.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ layout.name }}</td>
            </tr>
            <tr>
                <th>Timezone</th>
                <td>{{ layout.timezone }}</td>
            </tr>
            <tr>
                <th>LayoutType</th>
                <td>{{ layout.layoutType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_layout_index') }}">back to list</a>

    <a href="{{ path('app_layout_edit', {'id': layout.id}) }}">edit</a>

    {{ include('layout/_delete_form.html.twig') }}
{% endblock %}
